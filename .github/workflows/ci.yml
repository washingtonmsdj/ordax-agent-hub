name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate JavaScript syntax
        run: |
          node --check src/main.js
          node --check src/preload.js
          node --check src/renderer.js

      - name: Validate package metadata
        run: |
          node -e "const p=require('./package.json'); if(p.name!=='ordax-agent-hub'||p.version!=='0.1.0-dev'||p.main!=='src/main.js') process.exit(1)"
